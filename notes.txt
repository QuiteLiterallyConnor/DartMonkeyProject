new serial command structure:

DEVICE_NAME.COMMAND_NAME("KEY": VALUE, "KEY": "VALUE")

ie. 

X_AXIS.SET_ANGLE("ANGLE": 45, "INTERPOLATION": "CUBIC", "SPEED": 10);

A_MOTOR.SET_DEFAULT("PARAMETER": "INTERPOLATION", "VALUE": "LINEAR");
A_MOTOR.OFFSET_SPEED("SPEED": 50, "RAMP_UP_RATE": 10);